export enum StatusEnum {
  Pending = "pending",
  Active = "active",
  Deleted = "deleted",
}

export enum GenderEnum {
  Female = "female",
  Male = "male",
  Other = "other",
}

export enum RolesEnum {
  Admin = "admin",
  User = "user",
}
